| Formatter                               | Description & Parameters                                                                                                                                                                                                                     |
| --------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `formatCurrency(value, options)`        | Applies a locale-aware currency formatting mask to a number. - `value`: `string                                                                                                                                                              | number`— The value to format (e.g., 1234.56 or "1234.56"). -`options`: `object` — Options (must contain 'locale').                                            |
| `formatDate(dateObj, options)`          | Formats a Date using the active locale's date pattern. - `dateObj`: `Date` — Date to format - `options` — _Documentation not found._                                                                                                         |
| `formatMobileNumber(str, options)`      | Aplica máscara de número móvel sensível ao locale. - `str`: `string` — Valor a formatar (ex.: "11987654321"). - `options`: `import('../../types/index.js').FormatterOptions` — Opções de formatação.                                         |
| `formatPostalCode(str, options)`        | Applies postal code formatting mask (locale-aware) to a string. Supports alphanumeric postal codes (e.g., UK: "SW1A 1AA"). - `str`: `string` — The string to be formatted (e.g., "09010140" or "SW1A1AA") - `options`: `object` — Options    |
| `formatTaxId(str, options)`             | Applies a locale-aware Tax ID formatting mask based on a 'type'. Supports alphanumeric tax IDs (e.g., UK NI: "AA 12 34 56 A"). - `str`: `string` — The string to format (e.g., "12345678901" or "AB123456C") - `options`: `object` — Options |
| `toAccessible(number, locale, options)` | Formats a number into an accessible text representation (number to words). - `number`: `number                                                                                                                                               | string`— The number to format. -`locale`: `string`— The locale to use ('en-US' or 'pt-BR'). -`options`: `object` — Options.                                   |
| `toParts(number, locale, options)`      | Formats a number into parts based on the locale. - `number`: `number                                                                                                                                                                         | string`— The number to format. -`locale`: `string`— The locale to use for formatting. -`options`: `Intl.NumberFormatOptions` — Options for Intl.NumberFormat. |
| `toPercentage(number, locale, options)` | Formats a number as a percentage. - `number`: `number                                                                                                                                                                                        | string`— The number to format. -`locale`: `string`— The locale to use for formatting. -`options`: `Intl.NumberFormatOptions` — Options for Intl.NumberFormat. |
